<div class="title-row">
  <%= link_to(admins_auction_items_path, class: 'back-link') do %>
    <%= t('auctions.back_to_auction_items') %>
  <% end %>
</div>

<%= form_for(@auction_item, url: admins_auction_item_path, html: { class: 'content-form' }) do |f| %>
	<%= render partial: "form", locals: { f: f } %>
<% end -%>